## Branch Overview
- main → Stable Spring Boot REST API
- v2_pagination_sorting → Added pagination, sorting & filtering
- v3_docker_containerization → Containerized app with PostgreSQL + pgAdmin via Docker Compose
